# .NET
bin/
obj/
*.user
design-time.db*

# Local dev state
.devconfig/
.devconfig-pid.txt
.devlibrary/

# Embedding-index build workspace (dump, models, per-model indexes, packed artifacts)
.artifacts/

# Frontend
node_modules/
frontend/dist/

# IDE
.vs/
.vscode/*
!.vscode/settings.json
.idea/

# Logs
*.log

# Local import test data
.devimport/
frontend/.claude/launch.json
TODO.md
/.agents
/.claude/skills
/.github/skills
.github/hooks/impeccable.json
.codex/hooks.json
.e2econfig/

# The built SPA. Produced by `npm run build` in frontend/ and copied here by the publish step;
# Kestrel also needs the directory to exist at startup, so a backend-only clone must create it.
/src/Maki.Api/wwwroot/
diff.txt
/piolium
