# TODO

A running list of things to check, fix, or add. Add items freely — newest at the top of each section. When something is done, delete it or move it to **Done**. After each completed item, push and commit the changes.

## To do

### High Priority

- **Improve scrobbler.** If possible, map how many pages of a volume has been read on Kavita to the pages available in a volume file. And thereafter know what chapter we are on. Then scrobble that. Also, if we know for example that chapter 1-8 are in volume 1, then when chapter 8 has been read also scrobble that we have read volume 1. So both chapter and volume scrobbling for all titles. And where possible, we extract what chapter we are on in a volume.
- **Activity page changes.** Activity page should always show history, and progress for torrent downloads needs to be updated more regurlary. A manga download can take only a minute so having it update so sparsly makes no sense. Also make it paginated so we can see all the history.
- **Improve imports.** Messages sometimes show so fast that you cannot see them. For example, trying to import something when it is already in the library just silently fails. Instead, if we have it in the library but no chapters are downloaded, just import the files again without readding it to the library.
- **Add the possibility to manually link chapters to a specific file.** Be able to bulk select chapters on the series page and link them to a .cbz file in the series folder. Can be good when you have manually grabbed a compilation cbz that contains certain chapters and Mangarr can't automatically realize it based on the filename. Or if a chapters filename is weird and needs a manual link.

### Medium Priority

- **More statuses.** Have more status for example on a series, instead of Missing and Downloaded. We could have queued for the ones about to get downloaded. Can probably have more at other places where we have statuses, feel free to find and improve other areas as well.
- **Monitor status on main library page.** Show the monitor status on all series cards.
- **Make the "Search releases (Prowlarr)" modal larger.** Long titles gets cut off so it is hard to know what the release contains.
- **Make the api much more memory friendly.** Sometimes the memory can build up to over 500MB. I would like to keep it at around MAX 200-300MB.
- **Draft a new name.** A lot of somewhat similar services are already named Mangarr (a use of the *arr* words used by the likes of Sonarr and Radarr). It would probably be better to rebrand to something different. Come up with good laternatives, does not have to be anything "arr".
- **Improve filters for discovery.** Add things like genre, chapter count, etc

### Low Priority

- **Replace all metadata hotlinks with icons.** Change all the hotlinks from text (example MB for MangaBaka) to their respective icons.
- **Update readme.** Update readme to include everything, and also add a section on how to build a docker image.
- **Fix download button text top right.** The numbers are cut off by what looks like a radius.

## Known issues / to investigate

_(empty)_

## Done

- **Scrobble status on the series page** (2026-07). Every library series detail page now has a **Scrobbling** section showing, per connected tracker, the last chapter/volume pushed, the reading state (Reading/Completed/Plan to read/Listed), when it synced, a deep link to the remote entry, and a connection dot — plus a **Sync now** button. Backend: `GET /api/v1/series/{id}/scrobble` (`SeriesController.Scrobble`) links the library series to its Kavita counterpart the *same way the sync engine matches* — punctuation-normalized title / folder name (`ScrobbleMatching.NormalizeTitle` over `Series.Title` + `FolderName`) — so no new bookkeeping is stored. It discovers the Kavita series id from any scrobble row carrying that title (sync states, unmatched rows, **and mappings** — a review/manual match carries a title but may have no sync state yet, so a just-resolved series shows immediately), then returns a `SeriesScrobbleDto` with per-tracker `{connected, remoteId, method, url (tracker.EntryUrl), chapter, volume, status, syncedAt, error, reviewReason, reviewCandidates}`. Cheap: early-returns with zero tracker probes when Kavita isn't configured and there's no data; tracker auth checks are settings/token DB reads only (no network). **Needs-review is resolvable in place:** when a series has no confident match for a tracker, the row shows a yellow "Needs review" badge with the candidate list — each with a **Use** button, plus a paste-id/URL **Link** field and **Ignore** — reusing the existing `/scrobble/match` and `/scrobble/ignore` endpoints (so the Scrobble page's review queue and the series page stay in sync; both invalidate `series-scrobble`). Ignored trackers render an "Ignored" badge. The whole section hides itself when scrobbling isn't set up and the series has no scrobble data. Frontend: `useSeriesScrobble`, `SeriesScrobbleSection` (below Chapters, above Files). The **seamless, library-driven matching** the item also asked for was already in place — `ScrobbleService.ResolveAsync` prefers Mangarr library cross-ids (MangaBaka/AniList/MAL) before any remote search and derives the missing ids from the ones the library has, so library series match with no review; this change surfaces that per series and makes the rare review case fixable without leaving the page. Verified E2E on the dev backend (seeded): Frieren rendered "MyAnimeList · Ch. 118 · Completed" and "MangaBaka · Ch. 120 · Vol. 12 · Reading"; Hajime no Ippo rendered the AniList "Needs review" row with both candidates + Use/Link/Ignore and a connected MangaBaka "Not yet synced" row; clicking **Use** created the mapping, drained the review, and (after the discovery fix) the row immediately showed the mapped id + remote link; the curly-apostrophe title normalized-matched correctly; no console errors. Scrobble unit suite 27 green; dev DB restored to its pre-test state.
- **Discovery detail card + post-add navigation** (2026-07). Every Discover card is now clickable and opens a rich detail modal (`DiscoverDetailModal`), and adding a series jumps straight to its library page. Backend: `MangaBakaLocalStore.GetDetailAsync` returns a `MangaBakaDetail` from the local dump — full description, native/romanized titles, larger `cover_raw_url` poster, type/status/year/chapters/volumes, MangaBaka rating **plus per-source normalized ratings** (AniList/MAL/MangaUpdates/Kitsu from `source_*_rating_normalized`), authors/artists/publishers (publisher objects → names), genres, and **categorized tags from the dump's `tags_v2` column** grouped by MangaBaka's own `weight` bucket (`core`/`defining`/`recurrent`/`incidental`, exactly like the MangaBaka site; `is_genre`/`unweighted`/genre-duplicate tags dropped, each tag carries its description for a tooltip; **spoiler tags (`is_spoiler`) are blurred until hover/focus** exactly like the MangaBaka site, with a "Spoiler — hover to reveal" tooltip and keyboard focusability), and cross-provider metadata links. `GET /api/v1/recommendations/detail/{id}` (follows merged rows). **MAL reviews:** new `JikanReviewClient` fetches 2–3 reviews via Jikan (unofficial MAL API, no auth) at `GET /api/v1/recommendations/reviews/{malId}` — lazy, cached 12 h, rate-limited (2/s), spoilers filtered, **preliminary reviews kept** (ongoing/hiatus titles only have those), and fully best-effort (any failure → empty list, UI shows "No reviews found"). Frontend: `useRecommendationDetail`/`useMangaReviews`; cards became clickable buttons (keyboard-accessible) that open the modal; the quick **Add** button and the whole card both open it; each bucket caps at 18 tags with a "+N more" note so pathological cases (One Piece ~90 incidental tags) stay readable; the modal has inline root-folder + monitor + Add controls that on success navigate to `/series/{id}`, and shows a teal **View in library** button (plus an "In library ›" card badge) that navigates when the series is already owned. Verified E2E on the dev backend (Frieren detail: 39 categorized tags, 4 source ratings, all cross-links, 831-char description; One Piece modal rendered all sections with tag caps working — Core 8, Defining/Recurrent/Incidental 18 +N more; adding the Dandadan one-shot navigated to `/series/20`, then deleted to restore dev state; no console errors). **Note:** Jikan/MAL was in an upstream 504 outage during testing, so live review *rendering* couldn't be positively verified — only the graceful-empty path (endpoint returns `[]`, "No reviews found"); the parse path is straightforward and defensive. **Visual redesign** (follow-up, same month): the wide horizontal list cards were replaced with **poster-forward cards** matching the library's `cover-card` aesthetic (2:3 cover as hero, bottom scrim carrying a brand-tinted reason line, title, and year·status·chapters meta, plus a gold-star rating chip). A dense responsive grid (`POSTER_COLS` 2→6 columns mobile→desktop), redesigned section headers (ThemeIcon + title + count badge), poster skeletons while loading, and a compact active-filter chip summary shown under the header when the Customize panel is collapsed. Each card has a hover-revealed round **＋** quick-add control (top-right of the poster) that opens the detail modal; owned series instead show a persistent teal check that navigates to the library page. New `.discover-card`/`.discover-corner`/`.discover-meta`/`.discover-reason` CSS in `theme.css`. Verified E2E: poster grid renders at 2:3, responsive columns confirmed (2 at 375px, 6 at 1440px), rating/reason/meta all present, add control hidden at rest (opacity 0) and revealed on hover, detail modal still opens with all sections, no console errors.
- **Handle rate limiting** (2026-07). Sometimes when downloading a lot of chapters the source site will rate limit us. Instead of just failing all remaining downloads, keep them in the queue but put a rate limit cooldown before trying again.
- **File list view on the series page** (2026-07). New collapsible **Files** section on the series detail page listing every raw CBZ in the series folder cross-referenced with the DB, so failed/unmatched imports are visible and volume compilations show the chapters they back. Backend: `GET /api/v1/series/{id}/files` (`SeriesController.Files`) unions the series' `ChapterFile` records with an on-disk `*.cbz` scan and returns `SeriesFileDto` per file — `status` is `linked` (≥1 chapter mapped), `unlinked` (recognized name but no chapter — a failed/ambiguous import), `unrecognized` (name doesn't parse), or `missing` (DB record whose file is gone from disk); plus `parsedLabel` ("Ch.148"/"Vol.3"/"Vol.1-2"), `isVolume`, `sourceName`, `size` (live from disk when present), and `mappedChapters` (formatted, sorted chapter numbers — the whole point for volume files). Frontend: `useSeriesFiles(seriesId, enabled)` (lazy — only fetches when the section is expanded; invalidated on rescan), `SeriesFilesSection` component with a toggle, a "N · M need attention" count, per-row status badges (teal/yellow/orange/red + icon), an indigo Parsed badge for volume files, and a byte-formatted size. Verified E2E on the dev backend (Frieren) across a folder seeded with all states: a single-chapter file (`Ch.140.1` → Linked → "Ch. 140.1"), a `v03` compilation (→ Linked → **"Ch. 21, 22, 23"**, indigo VOL.3 badge), an unrecognized file (`random extras bonus.cbz` → Unrecognized, no mapping), and a deleted-from-disk record (→ Missing, `onDisk:false`) — API and rendered table both matched exactly, no console errors. Dev state restored (files deleted + rescan → back to 1 linked file). Full suite 217 green.
- **Volume ↔ chapter mappings made visual** (2026-07). On the series detail page, chapters whose backing file is a volume/compilation CBZ now show a distinct indigo **Vol.x** badge beside "Ch.y" — so it's clear at a glance which volume file each chapter was mapped from. Crucially this works for **scrape-source chapters that carry no volume metadata** (`Chapter.Volume` is null): the badge is driven by the *linked file's* parsed volume, not the chapter's metadata volume. Backend: `ChapterController.List` now materializes the rows then computes `FileVolume` per chapter via `ReleaseNameParser.ParseFileName` over the linked file's path (parsing can't run inside the EF query), emitting the volume label ("3", or "1-2" for range files like `v01-02`) only when the file `IsVolume`, else null. Frontend: `ChapterDto.fileVolume`; `chapterLabel` prefers `fileVolume` over metadata `volume`; the Chapter cell renders a tooltip'd `Vol.x` `Badge` (indigo) before `Ch.y` when `fileVolume` is set. Verified E2E on the dev backend (Frieren): created a `v03` compilation CBZ with embedded page markers `c021`–`c023` (chapters that were Missing with null volume metadata) → rescan linked ch 21/22/23 to the file with `fileVolume:"3"` while metadata `volume` stayed null, neighbours 20/24 stayed Missing/null; the series page rendered "VOL.3 · Ch.21/22/23 · Downloaded" for exactly those rows and plain "Ch.20"/"Ch.24" for the neighbours, no console errors. Dev state restored (file deleted + rescan → 0 volume-backed). Full suite 217 green.
- **Torrent import adopted the wrong (previous) series** (2026-07). After a series finished importing via qBittorrent, grabbing a torrent for a *different* series could re-import the first series into the new folder. Cause: `CompletedDownloadJob.ClaimTorrent` matches a hashless (.torrent) grab to a qBittorrent torrent purely by a time window, and the "already claimed" exclusion set was built **only from pending queue items** — a previously-imported download (item `Completed`) keeps seeding in qBittorrent but was no longer protected, so a new item could claim it and, since it's already complete, import it immediately into the wrong folder. Fix 1 (definitive): `claimedHashes` is now built from **every** torrent queue item's recorded `TorrentHash` (all statuses, `OrdinalIgnoreCase`) — a finished download's hash is persisted on its item at claim time, so it can never be re-claimed by another item. Fix 2 (hardening): `ClaimTorrent` now **prefers** an in-window torrent whose name carries the series' primary title tokens (`SearchQuery.Candidates(title).Last()` → the part before a subtitle separator, so "Frieren: Beyond Journey's End" matches a "Frieren v01" release), falling back to the original oldest-in-window guess when none match — a strict improvement that disambiguates concurrent grabs without ever stranding an odd-named release. Verified against the real compiled methods (reflection harness, 11 cases): the reported scenario now claims the correct in-flight torrent not the completed previous one; series-name preference beats an older unrelated in-window torrent (incl. subtitled titles); single non-matching candidate still claims (no stranding); out-of-window never claims. Full suite 217 green. (No live qBittorrent E2E — the job has no on-demand trigger and needs a real client; the pure claim/match logic was exercised directly instead.)
- **Volume chapter linking missed chapters on rescan** (2026-07). Some chapters that a volume/compilation CBZ demonstrably contains (correct `cNNN` page markers) stayed **Missing**, and rescanning didn't fix them (e.g. Boyish Girlfriend ch 14–16 in the vol 2 file). Cause: rescan's relink retry only reprocessed files that **no** chapter pointed at, and the adopt path only content-scanned a volume file when its range match found **zero** chapters — so a file that linked *some* chapters (e.g. a MangaDex volume-range match covering only part of the compilation, whose boundaries disagree with the scanlation's) never got its remaining chapters linked, and neither did chapters that appeared in a later refresh. Fix: new `CbzLinkService.FillVolumeContents` scans every volume file's embedded page markers and links any chapter the file contains that is **still unlinked** — running on already-linked volume files too, and only ever touching `ChapterFileId == null` chapters so it never steals from another file. Wired into both `LinkFilesAsync` (after the volume-backfill retry) and `RescanSeriesAsync` (over all on-disk volume files, which the unlinked-only retry skipped). Verified E2E on the dev backend (Frieren): a compilation CBZ with markers c001–c006, pre-linked to only ch 1–3 (the exact "partially linked" state the old rescan skipped), → rescan reported `relinked: 3` and linked ch 4/5/6 to the same file, ch 7 (not in the file) stayed missing, and a second rescan was idempotent (`relinked: 0`); log confirmed "Linked 3 previously-missing chapter(s) … from its embedded page names". Dev state restored afterward.
- **"Monitored" filter on series page** (2026-07). The chapter SegmentedControl on the series detail page gained a "Monitored (N)" option between "All" and "Missing" that shows only chapters where `monitored` is true. One predicate added to `chapterFilters` (`monitored: (c) => c.monitored`) plus the corresponding segment with a live count — mirrors the existing Missing/Downloaded/Specials filters exactly. Verified in the browser on Dandadan (all 254 monitored → count reads 254, filter applies with no console errors).
- **Path mappings for qBittorrent** (2026-07). When qBittorrent runs under a different mount than Mangarr (e.g. in Docker it reports `/downloads/x.cbz` while Mangarr sees `Z:\downloads\x.cbz`), completed-torrent imports failed with "Download path not accessible". The Kavita path mapper — which already did exactly this rewrite — was generalized to `Mangarr.Core.Paths.PathRemapper` (moved verbatim, `KavitaPathMapper` deleted and its two `KavitaScanService` call sites + the `KavitaClient` doc-cref + the test repointed). `CompletedDownloadJob.ImportAsync` now maps `torrent.ContentPath` through it before the existence check, using new settings `qbittorrent.pathmapfrom`/`qbittorrent.pathmapto` (fetched via `ReleaseService.GetQbtPathMapAsync`). Settings plumbed end-to-end: `QBittorrentSettings` record + `GET/PUT /settings/qbittorrent` gained the two fields, and the qBittorrent card on the settings page got "Path mapping — qBittorrent side / Mangarr side" inputs (blank = no rewrite, same UX as the Kavita card). The mapper is unchanged logic so the existing Kavita behavior is identical; 3 new qBittorrent-direction cases added to the (renamed) `PathRemapperTests` (16 total). Verified E2E on the dev backend: `GET/PUT` round-trips the path map (`/downloads` → `/mnt/user/downloads`), and the settings page renders both new fields with the right description (caught + fixed a JSX gotcha — `\\` in a double-quoted *attribute* is two literal backslashes, unlike a JS string literal). Full-path import rewrite itself relies on the unit-tested mapper (a live Docker qBittorrent wasn't stood up). **Note:** left the pre-existing uncommitted `QBittorrentClient.cs` login-check change untouched — it looks wrong (qBittorrent's `auth/login` returns 200 `Ok.`, not 204) and is unrelated.
- **Improved chapter scanning on volumes — read chapters from page file names** (2026-07). A volume/compilation CBZ now links to the chapters it *actually contains* by reading the chapter markers embedded in its page file names (scanlation compilations name pages like "Boyish Girlfriend - c049 (v05) - p113 [web] [Manga UP!] [Oak].png" → chapter 49). New Core `VolumeChapterScanner`: `ScanCbz` opens the archive (central-directory only, no decompression), filters to image entries, and a regex (`(?<![a-z])c(?:h(?:apter)?)?\.?\s*(\d+(?:\.\d+)?)`) pulls distinct chapter numbers from each page name — the "c"-prefix, letter-lookbehind marker ignores the `v05`/`p113` volume/page markers and won't misread "Arc049"/"Comic". `CbzLinkService.LinkVolumeByContents` links each scanned number to its Chapter row; it runs as a fallback whenever a volume file range-matches **nothing** (both the adopt path in `LinkFilesAsync` and rescan's retry loop), i.e. exactly when the chapter rows carry no volume info (scrape sources) or the compilation's boundaries disagree with the metadata provider's — so the existing volume-range/MangaDex-backfill path is untouched when it already works. Linked chapters are then excluded from the completed-series proportional estimator. 14 new unit tests (marker variants, dedup/sort, decimals, false-positive rejection, real-archive round-trip). Verified E2E on the dev library (Frieren): a `v99` compilation (a volume number MangaDex maps no chapters to, forcing the content-scan branch) with embedded markers c021–c023 → rescan linked exactly chapters 21/22/23 to the file (neighbours 20/24/25 stayed missing); a second rescan was idempotent; deleting the file and rescanning unlinked them, restoring state.
- **Recommendation engine v2 — Phase 4: per-seed attribution + semantic reason lines** (2026-07). A semantic pick now names the *specific* seed whose "feel" drove it instead of just listing shared genres. `SemanticRecommender` still ranks against the mean seed vector (ranking unchanged), but after picking the winners it fetches each seed's individual vector (`EmbeddingStore.GetVectors`) + title (from the dump) and attributes every winner to its single most-similar seed (`EmbeddingMath.MostSimilar`, pure + unit-tested), exposed as `MangaBakaRecommendation.BecauseOfTitle`. The Discover reason line reads "Feels like {seed} · {shared genres}" for semantic hits (genre-only fallback picks keep "Because: …", relations keep "sequel/spin-off of X"). Genre-only hits leave `BecauseOfTitle` null so the fallback path is unchanged. Caching by seed+filter+obscurity signature was already done in Phases 2–3. Verified E2E against the dev vector index: two seeds (Frieren + Ippo) → each pick attributed to the closer seed (Berserk/Seven Deadly Sins/FMA → Frieren; One Piece/Claymore/Fairy Tail → Ippo); whole-library seed → picks attributed to the closest specific library title (One Piece → Ippo; Air Gear/Negima/Berserk → Dandadan; Freezing → FranXX), each with genres alongside; and the Discover page rendered "Feels like Hajime no Ippo… · Comedy, Shounen, Drama" etc. in the browser while the "Related" section kept its relation reasons. 2 new unit tests (`MostSimilar`). **Testing note:** the user's real backend held port 8990, so the new code was built to a scratch output dir and run on 8991 against `.devconfig` (Port temporarily flipped in dev config + vite proxy, both reverted) — never touched the real backend or library.
- **Recommendation index settings + status** (2026-07). New "Recommendation index" card in Settings (below Metadata) showing the semantic-index state and driving the build. Backend: `EmbeddingIndexStatus` (thread-safe live snapshot — running/phase/embedded/scanned/started/finished/lastError) written by `SeriesEmbeddingIndexer` as it runs; `GET /settings/recommendations` reports model-present, dump-present, vector count, recommendable total (counted once when idle and cached on the status object so polls stay cheap), and the live snapshot; `POST /settings/recommendations/build` triggers the Quartz `EmbeddingIndexJob` on demand (no-op if already running). UI polls every 2 s while running (react-query `refetchInterval`), shows a progress bar + "Indexing… N / total", and a Build/Rebuild button (disabled while running or if the dump isn't downloaded). Verified E2E: build trigger returned `{started:true}`, live status advanced (embedded 192→576→5280, total 95,668), and the settings card rendered the live count + progress bar (fresh fetches showed 2,144→6,336 — the auto-poll interval is throttled in the occluded preview pane but works in a real browser). **Gotcha:** the recommendable-total COUNT scans the ~300k dump (~1–2 s), so it's computed once and cached on the status object rather than per poll.
- **Recommendation engine v2 — Phase 3: obscurity dial** (2026-07). A mainstream↔hidden-gems slider on Discover (−1…0…+1) biases picks by popularity. `RecommendationRequest.Obscurity` flows into `SemanticRecommender`, which reads each candidate's `popularity_global_current` (a global rank, 1 = most popular) and adds an obscurity term to the hybrid score (`EmbeddingMath.HybridScore` gains `obscuritySlider · (percentile − 0.5) · weight`, weight 4.0, unit-tested). **Key fix:** a linear rank→percentile barely moved anything because ~all good candidates cluster at rank < 2000 (percentile < 0.01) — the term was nearly constant. **Log-scaling** the rank (`log(rank)/log(maxRank)`, max cached per process) spreads the popular cluster so the dial actually reorders it. UI: an "Obscurity" Slider in the Customize panel (popular ↔ balanced ↔ gems), sent only when off-centre. Verified E2E on the dev index: seed=Frieren, mainstream(−1) → Berserk/One Piece/AoT on top (median popularity rank 105); hidden(+1) → Ciel/Buddha/Kubera rise, One Piece/AoT drop out of the top (median rank 265, mean 1292→2217); neutral unchanged. Slider renders in the panel; obscurity=0 skips the popularity work entirely.
- **Recommendation engine v2 — Phase 2: seeding & filters** (2026-07). Discover can now be seeded from a chosen subset of the library **or any title searched from MangaBaka** (not just the whole library), and constrained by filters. `POST /recommendations` replaced the GET and takes a body: `{ seedIds?, filters?, refresh? }` where `filters = { yearMin, yearMax, types[], statuses[], minRating }`. `RecommendationFilters` (Metadata) builds a parameterized WHERE fragment spliced into both the semantic and the genre-fallback candidate scans; `SemanticRecommender.GetSimilarAsync` and `MangaBakaLocalStore.GetSimilarAsync`/`GetRelatedAsync` were generalized from `libraryIds` to `(seedIds, excludeIds)` so seeds and the always-excluded owned set are separate (owned series never appear in results even when seeded from something else). `RecommendationService` defaults seeds to the whole library, caches by the seed+filter signature, and the "related" (relationship) section follows the seeds too. **UI:** a "Customize" panel on Discover with a searchable seed MultiSelect (library titles merged with live MangaBaka search results — labels cached so selected out-of-view seeds stay labeled), a year RangeSlider, min-rating Slider, and type/status MultiSelects, plus Apply/Reset; headings switch to "Related to your seeds"/"Feels like your seeds" when custom-seeded. 6 new unit tests for the filter SQL builder. **Verified E2E** against the real dev dump with a partial vector index (semantic path live): API — seed=Frieren → fantasy (Seven Deadly Sins, Claymore, Delicious in Dungeon); seed=Frieren + minRating≥85 + type=manga → all high-rated fantasy; seed=Ippo + year 1980–2000 → every result in-range (Slam Dunk '90, JoJo '86, One Piece '97) and still sports/fighting; logs confirm "(semantic)" mode. UI — expanded Customize, the seed dropdown correctly merged library + MangaBaka search hits, selecting Frieren + Apply switched the heading to "Feels like your seeds" and the grid to fantasy titles, no console errors. (Mantine sliders/MultiSelect are hard to drive via pointer in the preview pane — the seed path was driven through the real UI; filters were proven at the API.)
- **Recommendation engine v2 — Phase 1: semantic ("feel") matching** (2026-07). Discover now recommends by semantic similarity of descriptions, not just shared genre labels. A local **int8-quantized bge-small-en-v1.5 ONNX** model (384-dim BERT, ~34 MB, downloaded to `{ConfigDir}/models/` on first use like the dump) embeds each series' `title + genres + description`; a background Quartz job (`EmbeddingIndexJob`, +4 min after boot, then daily) precomputes vectors for every rated/active/non-novel/safe series (~96k rows, ~30 min one-time on CPU at ~60/s; incremental after — only re-embeds when text or `ModelVersion` changes, tracked by a per-row content hash) into a separate `embeddings.db` (survives the nightly dump swap). `SemanticRecommender` builds a seed vector (mean of the library's embeddings), ATTACHes the dump to the vector DB, scans embedded candidates, and ranks by a **hybrid score** (`EmbeddingMath.HybridScore`, unit-tested): semantic cosine leads (×3), refined by the existing genre/tag/author/rating signals. `RecommendationService` uses it once ≥1000 vectors exist and **falls back to the v1 genre scan** while the index builds (so Discover never breaks). All in-process (`Microsoft.ML.OnnxRuntime` + `Microsoft.ML.Tokenizers`, CPU; a GPU ORT build would drop in with no code change) — no Python, no training, no per-user data. **Design decisions** (ONNX in-process over Python sidecar / trained model; quantized model; CPU default): [approved plan](https://claude.ai/code/artifact/e6f24ff9-90c9-46a9-8a86-05b69bbc3e4a). Phase 1 is UI-free — reason lines still show shared genres; seeding/filters/obscurity are Phases 2–4. New Metadata layer `Embedding/` (math, options, model store, embedder, vector store, indexer, recommender); 16 new unit tests (math + store round-trip); model I/O and CLS-pooling validated against the real model. **Verified E2E** against the real dev dump + model: indexed 25k, all 5 library seeds embedded, and single-seed queries proved the "feel" — **Frieren alone → fantasy/adventure** (Delicious in Dungeon, Claymore, Kubera), **Hajime no Ippo alone → Ashita no Joe as the #1 pick** plus Slam Dunk / All-Rounder Meguru / other boxing-combat titles that pure genre matching (Slice-of-Life/Sports tags) would never surface above generic shounen. Backend smoke: boots with the new DI, `/recommendations` returns 200 in "(genre)" mode until the index lands. **Gotchas:** int8-quantized bge-small is ~26× faster on short text and ~identical similarity quality vs fp32 (0.75 vs 0.76 for related pairs) — the right CPU default; ONNX I/O is `input_ids`/`attention_mask`/`token_type_ids` → `last_hidden_state`, bge wants **CLS-token (position 0) pooling + L2 normalize**; `BertTokenizer.Create(vocab)` + `EncodeToIds` gives `101…102`; the model-size floor guard must sit below 34 MB (quantized) or `EnsureAsync` re-downloads forever.
- **Unmonitored chapters excluded from the total count** (2026-07). The library card and series-detail header showed e.g. `39/40` when the only "missing" chapter was an unmonitored special the user had deliberately skipped. The total (denominator) now counts only chapters that are `Monitored || hasFile` — an unmonitored, un-downloaded chapter no longer inflates the total, so a fully-downloaded series reads `39/39`; downloaded chapters always count so the numerator can never exceed the denominator, and a monitored-but-missing chapter still shows as `39/40`. Fixed in `SeriesController` for both the list and single-series endpoints, and in `SeriesDetailPage`'s client-side header count. Verified E2E on the dev library: unmonitoring a monitored, file-less Dandadan chapter dropped both counts 15/254 → 15/253 (file count unchanged) and re-monitoring restored 15/254.
- **Clickable metadata links** (2026-07). Every cross-provider id a series has now renders as a clickable badge that opens the site in a new tab. Backend: `SeriesWebLinks.Labeled(series)` (Core) returns `MetadataLink(site, url)` pairs for MangaBaka / AniList / MyAnimeList / MangaUpdates / MangaDex (the existing `For`/`Joined` ComicInfo helpers now derive from it, so there's one source of truth); `SeriesDto.Links` exposes them. Frontend: shared `MetadataLinks` component renders the links as Mantine `Badge`s that call `window.open(..., '_blank', 'noopener,noreferrer')` on click — deliberately **not** anchors, so they can sit inside the library card's `<Link>` without invalid `<a>`-in-`<a>` nesting; the click `stopPropagation`s so it opens the site instead of navigating to the series page. Placed compact on library grid cards (short labels MB/AL/MAL/MU/MD) and full-label on the series detail header. Discover cards get a MangaBaka badge built from the recommendation's `providerId`. Verified E2E on the dev library: API returns fully-populated `links` for all 5 series; library cards render 25 badges (5×5) with correct accessible names, clicking MangaBaka opened `mangabaka.org/601` without navigating to the detail page; detail page shows the full row; Discover MangaBaka badge opened `mangabaka.org/{providerId}`; zero nested anchors and no console errors. 137 Core tests still green.
- **Scrobbling ("Scrobble" tab) — native MangaScrobbler port** (2026-07). Kavita reading progress → AniList / MyAnimeList / MangaBaka, any combination. **Engine** (`ScrobbleService`, Quartz `ScrobbleJob` ticking every minute so interval changes apply without restart; default 30 min, force-trigger via `POST /scrobble/sync`): polls Kavita over the existing shared connection settings, computes the highest *fully-read* chapter/volume per series from chapter-level pagesRead (`KavitaProgress`, Core — the series-level aggregate is stale; specials and ≥10000 sentinels ignored; volume-only releases advance the volume counter), then pushes **forward-only** updates (`ScrobblePlanner`, Core, pure + unit-tested): pushed value is max(remote, kavita), completed entries never demoted, user statuses (paused/dropped/planning → "other") only moved back to reading when Kavita shows *further* progress, completion set when the site's total chapters (or volumes, when chapters unknown) is reached, optional plan-to-read listing for unread series (never touches existing entries). **Matching precedence** (better than the original): saved mapping (incl. manual/ignored) → **Mangarr library cross-ids** (Kavita name matched to `Series.Title`/`FolderName` punctuation-normalized — series Mangarr manages scrobble instantly with zero remote calls) → Kavita web links (which Mangarr itself pushes) → cross-derivation (MangaBaka `/v1/source/*`, AniList `idMal`) → strict title search (Ratcliff/Obershelp ≥ 0.93, ported difflib); anything else lands on a **Needs review** list with candidates (Use/paste-URL-or-id/Ignore). **Trackers** (Core, `IScrobbleTracker`): AniList GraphQL + OAuth code flow, MAL API v2 + OAuth PKCE-plain with auto refresh (~31-day tokens), MangaBaka REST via PAT (PATCH→POST-on-404 library upsert; TEXT/fractional numbers coerced); endpoints env-overridable (`MANGARR_SCROBBLE_*`) for mock E2E. **OAuth**: `GET /scrobble/auth/{service}/start` returns the authorize URL (redirect URI derived from the request origin, shown in Settings for app registration); callback `GET /api/v1/scrobble/oauth/{service}` is API-key-exempt (the provider redirects the browser there) and authenticated by the random per-session `state` instead; wrong state → error redirect. **Storage**: `ScrobbleToken/Mapping/SyncState/Unmatched/Log` tables (`Scrobbling` migration; log capped at 500). **UI**: Scrobble nav page (connection cards with Connect/Disconnect, sync-now + last/next meta, needs-review cards, recent-syncs table, activity log, 5 s poll) + Settings "Scrobbling" card (client ids/secrets, PAT, interval, library-id filter, plan-to-read switch, per-service redirect-URI hints). 34 new unit tests (planner/matching/progress incl. Kavita's string-typed chapter numbers). Verified E2E against mock Kavita+MangaBaka+MAL servers: full MAL OAuth round-trip (PKCE + state, anonymous callback, username fetched), library-match (Ippo → MAL 7/MangaBaka 206 from Series cross-ids), weblink match, forward-only (remote ch 100 kept over Kavita ch 12), completed via TEXT `"12"` total, PATCH-only update vs PATCH→POST insert bodies asserted, repeat sync = "0 updated, 8 up-to-date" with zero tracker writes, manual match by pasted URL + ignore + invalid-id 400, needs-review drained, UI sync-now button; Scrobble/Settings pages verified in the browser (one hydration bug found via console — Badge inside `<p>` — and fixed). **Mock gotcha:** `PostAsJsonAsync`/`JsonContent` sends chunked bodies; python `http.server` mocks must decode Transfer-Encoding: chunked themselves or the body reads empty.

- **Recommendation engine ("Discover" tab)** (2026-07). New nav page recommending series from the local MangaBaka dump, in two sections. **Related to your library:** direct relations of library series read from the dump's `relationships_sequel/prequel/spin_off/side_story/main_story` JSON id columns (merged entries followed to canonical, max 3 hops). **Because of what you collect:** one full-table scan (~300k active rows, ~5 s cold / ~2 s warm, cached 12 h keyed on the library's MangaBaka id set; `?refresh=true` bypasses) scored against a library profile — genre weight 2×, tag weight 1×, same-author bonus 1.5, all multiplied by `(0.5 + rating/100)`; candidates need ≥2 matched genres or an author match; filters: `state='active'`, `rating IS NOT NULL`, `content_rating != 'pornographic'`, `type != 'novel'`. Top-N pruning keeps a floor score so no true winner is dropped; descriptions hydrated only for winners. Implementation: `MangaBakaLocalStore.GetRelatedAsync/GetSimilarAsync`, singleton `RecommendationService` (scoped DbContext via scope factory), `GET /api/v1/recommendations`, `DiscoverPage.tsx` with reason lines ("Spin-off of X" / "Because: Romance, Comedy"), rating badges, in-library detection by MangaBaka id, and the same add-modal flow as the Add page. Requires the local dump (400 otherwise, surfaced as a warning). Verified E2E on the dev library (Dandadan/Frieren/FranXX/Ippo): related section returned the Dandadan side story one-shot, a Frieren anthology, and a FranXX spin-off; similar section returned coherent shounen picks (One Piece, Chainsaw Man, FMA...); cache 4 ms, refresh recomputes. UI not browser-verified (Browser-pane tools still unavailable).
- **Better volume handling — chapter↔volume mapping from MangaDex** (2026-07). Volume CBZs now link to the chapters they contain even when the chapter rows carry no volume info (scrape sources don't have it). New Core capability `IChapterVolumeSource` (chapter number → volume map); `MangaDexSource` implements it via the feed with **`includeUnavailable=1`** — unlike the aggregate endpoint and the default feed, that flag still lists delisted chapters of licensed titles *with their volume assignment*, and no language filter is used since volume boundaries are language-independent (the EN feed of a licensed title is empty). `CbzLinkService` backfills `Chapter.Volume` from the first volume-capable enabled mapping whenever volume-parsed files remain unlinked (both the adopt path and rescan's retry path), then relinks by the existing exact volume-range matching. The completed-series proportional estimator now also accepts `Cancelled` status (fixed chapter count, same certainty). `ChapterSyncService` additionally populates `Series.MangaDexUuid` from the mangadex mapping (it was never set — MangaBaka has no MangaDex ids — so MangaDex web links were silently missing). Verified E2E in dev replicating the real library's failing case (My Dress-Up Darling: licensed, completed, 15 volumes known, v01–v14 on disk): rescan backfilled 131 chapter volumes from MangaDex and linked every chapter of vols 1–14 to its exact file, vol 15's chapters correctly stayed missing; MangaFire's 216 off-numbering extra rows stayed volume-less as expected. **Also fixed in passing:** `ChapterFile` had no FK to `Series`, so deleting a series orphaned its file rows (the real DB had rows for 31 deleted series) — `ChapterFileSeriesCascade` migration adds the cascade and deletes existing orphans first; verified live on both DBs.
- **Loosened Prowlarr search + manual query input** (2026-07). Release search no longer requires the full title to match. Auto mode builds progressively looser queries via `SearchQuery.Candidates` (Core, unit-tested): full title with typographic punctuation normalized (curly quotes/dashes → ASCII, whitespace collapsed — indexers rarely store "’"), then the part before a subtitle separator (`:`, ` - `, `~`; skipped if <2 chars so "K: Return of Kings" doesn't search "K"); each is tried until one returns torrent results. `GET /api/v1/release` gained an optional `query=` param (manual queries go through verbatim, no fallback) and now returns `{query, releases}` — the query actually used is echoed so the UI can show it. The release modal got a search input prefilled with the used query (so an auto-loosened search shows what it searched) plus a Search button/Enter to re-search; results cached per (series, query). Verified E2E against a recording mock Prowlarr with dev series "Hajime no Ippo: Fighting Spirit!": auto search issued `query=Hajime%20no%20Ippo%3A%20Fighting%20Spirit%21` then fell back to `query=Hajime%20no%20Ippo` which returned the release, response echoed the loosened query; manual `query=Ippo v05` was sent verbatim. (UI not browser-verified — Browser-pane interaction tools still unavailable this session.)
- **Prowlarr indexer/category options** (2026-07). Release searches can be restricted to specific Prowlarr indexers and Torznab categories. `ProwlarrClient.SearchAsync` appends repeated `indexerIds=`/`categories=` query params (same shape Prowlarr's own UI sends); new `GetIndexersAsync` pulls `GET /api/v1/indexer` incl. category capabilities. Settings stored as CSVs (`prowlarr.indexerids`/`prowlarr.categories`, empty = no restriction) via `GET/PUT /settings/prowlarr/options` — deliberately separate from `PUT /settings/prowlarr` so the connection card can't wipe them. `GET /settings/prowlarr/indexers` proxies the indexer list (subcategories flattened) for the UI: a "Prowlarr search options" card below the connection card with per-indexer checkboxes and a searchable category MultiSelect whose suggestions are the union of the indexers' capabilities (saved-but-unadvertised ids stay selectable). Verified E2E against a recording mock Prowlarr: proxied indexer list, saved options produce `...&indexerIds=1&categories=7030&categories=8000`, cleared options produce an unrestricted query, and a mock release round-trips into Mangarr's release list. Live check against the real Prowlarr still pending on credentials (see known issue).
- **Numbering-scheme clash detector** (2026-07). `NumberingClashDetector` (Core, unit-tested) runs on every chapter sync with the per-source number lists: flags a series when one source has ≥3 sub-chapter numbers (x.1–x.4, excluding ≥.5 omake and parts the source itself has whole) over ≥2 distinct integer parts that another source lists as whole chapters — thresholds calibrated on the real case ("Cute Pajamas": MangaDex 1.1–2.4 vs WeebCentral 1..14; the original ≥3-distinct-parts rule missed it). Stored in new `Series.NumberingClash` column (`SeriesNumberingClash` migration) as `"subSource|wholeSource"`, exposed on `SeriesDto`, rendered as a yellow alert above the source mappings on the detail page suggesting disabling one mapping. A clash is always recorded; clearing requires every enabled mapping to have fetched that run (a temporarily failing source can't wipe a real flag; disabling the clashing mapping clears it on the next refresh). Verified E2E on the real series: flag set on refresh with both sources, cleared after disabling WeebCentral.

- **Kavita metadata push (Komf-style)** (2026-07). After every scan-folder request (and on the on-demand `refreshmetadata` endpoint via a push-only queue), `KavitaScanService` pushes what ComicInfo.xml can't carry: the MangaBaka poster as the Kavita series cover (`POST /api/Upload/series`, base64, locked; skipped when the cover is already locked so user-set covers are respected — which also means a changed poster won't re-push until the user resets the cover), web links (MangaBaka/AniList/MAL/MangaUpdates/MangaDex from the series cross-ids), publication status (Hiatus/Cancelled are locked because ComicInfo can't express them; Ongoing/Ended stay unlocked so Kavita can upgrade Ended→Completed itself) and release year (locked). Admin endpoints use the JWT from `POST /api/Plugin/authenticate` (cached in `KavitaClient`, refreshed on 401). The push retries every 30 s (up to 8×, first attempt +60 s) because Kavita runs scan-folder scans ~1 min after they're requested. **Series lookup gotcha:** Kavita's series name is parsed from *file* names, so titles with filesystem-illegal chars differ ("Hajime no Ippo: Fighting Spirit!" vs folder "Hajime no Ippo Fighting Spirit!") — `FindSeriesAsync` ORs equals-filters for both title and folder name (`POST /api/Series/all-v2`), then verifies via the returned `folderPath`. Metadata patching round-trips Kavita's own `GET/POST /api/Series/metadata` JSON so unrelated fields are untouched. Also: ComicInfo `Web` now carries all cross-links comma-separated (shared `SeriesWebLinks` helper), so weblinks survive rescans without a lock (Kavita has no webLinksLocked). Verified E2E against Kavita v0.9.0.2: cover locked in (`series1.png`), webLinks/status/year all landed, second push correctly skipped the locked cover. Kavita test-setup gotcha: `POST /api/Library/create` needs `fileGroupTypes:[1]` (enum starts at 1=Archive) and `enableMetadata:true` if ComicInfo parsing matters.
- **Kavita integration — scan on new files** (2026-07). New `KavitaClient` (Core, `Mangarr.Core.Kavita`): connection test via `POST /api/Plugin/authenticate?apiKey=&pluginName=Mangarr`, scans via the anonymous `POST /api/Library/scan-folder` (`{apiKey, folderPath}`; Kavita schedules the actual scan ~1 min later). `KavitaScanService` (Api, singleton + hosted service) debounces: file events queue the series folder, a 30 s flush sends one scan-folder call per distinct folder — hooked into `ChapterDownloadProcessor` (after CBZ import), `CbzLinkService.LinkFilesAsync` (library import / torrent import / rescan-adopted files) and `UpdateComicInfoAsync` (when files were rewritten). Settings `kavita.url/apikey/pathmapfrom/pathmapto` (`GET/PUT /settings/kavita`, `POST /settings/kavita/test`, card on the settings page). `KavitaPathMapper` rewrites Mangarr-side paths to Kavita's view (Docker mounts / NAS): case- and separator-insensitive prefix match on a path boundary, separator style follows the target; unit-tested. Metadata flows to Kavita via the standardized ComicInfo.xml (already done) — no direct metadata push needed. Verified E2E against a real Kavita v0.9.0.2 (scratchpad install): settings card Test/Save through the UI, file adopted on rescan → debounced flush → path-mapped scan-folder call → Kavita scanned and the series appeared with all files. **Gotchas:** Kavita's "The directory ... does not exist" scanner error is a catch-all for any ArgumentException during the scan (mine was invalid `fileGroupTypes:[0,...]` in the test library — the enum starts at 1=Archive); Kavita also ignores dot-prefixed folders, so `.devlibrary` itself can never be a Kavita library folder (tests must mirror to a non-dot path).
- **Unmonitor specials + monitor modes + chapter filter** (2026-07). `NewChapterMonitorMode` gained `MainOnly = 2` (monitors whole-numbered chapters and one-shots, skips decimals; migration-free — column is int). Specials predicate lives on the entity: `Chapter.IsSpecial(number)` / `Chapter.MonitoredUnder(mode, number)` (culture gotcha: test InlineData decimals must parse with InvariantCulture — the dev machine's locale treats '.' as a group separator). `ChapterSyncService` honors the per-series mode for newly released chapters. `POST /series/{id}/monitormode {mode}` applies All/MainOnly/None to every existing chapter and persists the mode (does not touch `Series.Monitored`). New setting `monitoring.unmonitorspecials` (`GET/PUT /settings/monitoring`, switch on the settings page): when on, series created via Add or Import default to MainOnly. UI: monitor-mode select on the series detail page, "Monitoring" bulk action modal on the library page, and a chapter filter (All / Missing / Downloaded / Specials, with counts) on the detail page. Verified E2E on Dandadan (254 ch, 13 specials): MainOnly → 241/254 monitored, exactly the specials off; bulk restore to All; import with setting on → new series got MainOnly.
- **Link source by pasted URL + go-to button** (2026-07). The link-source modal accepts a pasted series-page URL: `ISource` gained a default `ResolveSeriesIdFromUrl(Uri)` (helper `SourceUrl.PathTail` handles host matching incl. www. and path-tail extraction), overridden by all four sources — MangaDex `/title/{uuid}[/slug]` (uuid-validated), MangaFire `/title/{hid}-{slug}` (first segment), MangaPill `/manga/{id}/{slug}` (full tail), WeebCentral `/series/{id}/{slug}` (full tail). `GET /api/v1/search/resolvesource?url=` finds the owning source, fetches the series detail, and returns name/title/cover (proxied); the modal detects `http(s)://` input, shows the resolved card with a source badge, and links on click. Unit tests in `ResolveSeriesIdFromUrlTests`. Each search result also got an ↗ button (stopPropagation) to open the source page in a new tab before linking. Verified E2E: slugged MangaDex URL → resolved card → linked with correct uuid.
- **ComicInfo toggle during import** (2026-07). "Import N selected" now opens a confirmation modal explaining ComicInfo standardization, with a default-checked "Standardize ComicInfo.xml" checkbox that resets to checked on every open. The flag rides `ImportRequest.UpdateComicInfo` → `LibraryImportService.ImportAsync` → new `updateComicInfo` param on `CbzLinkService.LinkFilesAsync` (torrent import and rescan keep the default true); when off, the per-file progress stage reads "Linking files" instead of "Updating ComicInfo". Verified E2E both ways: toggle off → imported CBZ byte-identical (MD5 unchanged, no updater invocation logged); toggle on → valid CBZ rewritten with standardized ComicInfo.xml. Note: the DARLING in the FRANXX files in `.devimport` are corrupt dummy zips ("Central Directory corrupt") — the updater warns non-fatally on them; use a real zip when a positive rewrite proof is needed.
- **Library bulk selection + full metadata refresh** (2026-07). Library page has a Select mode: per-card checkboxes, select all, and bulk actions run sequentially with a live progress notification — Search missing, Refresh chapters, Refresh metadata, Update ComicInfo, Delete (confirm modal with optional delete-files checkbox). Backing endpoints added: `POST /series/{id}/refreshmetadata` (full provider re-pull **including poster**, via new `SeriesMetadataRefreshService` shared with `MetadataRefreshJob`, which keeps its no-cover behavior) and `POST /series/{id}/updatecomicinfo` (`CbzLinkService.UpdateComicInfoAsync` re-standardizes every CBZ; Mangarr's own downloads are no-ops). Series detail page also got a "Refresh metadata" button. Verified E2E on the dev config. **Testing gotcha:** the embedded Browser pane never fires `requestAnimationFrame`, so Mantine `Modal`s render an empty root there (and `preview_screenshot` times out) — shim `window.requestAnimationFrame = cb => setTimeout(() => cb(performance.now()), 16)` via `preview_eval` before driving any modal; the modals work fine in a real browser.
- **Live per-row import progress** (2026-07). Library import now streams progress over SignalR (`importProgress` event): each stage broadcast from `LibraryImportService` (Fetching metadata, Renaming folder, Downloading cover, Finding sources, Syncing chapters) plus per-file counts during ComicInfo/linking via a progress callback on `CbzLinkService.LinkFilesAsync`, and a done/failed event per item from the controller. Import page shows a progress bar + status message on each row (queued rows show an empty bar, failures turn red with the error inline). Also fixed a race in `ensureConnection` (frontend signalr.ts) that opened duplicate hub connections when several components subscribed at once. `.claude/launch.json` gained a `backend-dev` config that runs against `.devconfig` via `dotnet run -e MANGARR_CONFIG_DIR=...` — use it instead of `backend` for destructive testing. Verified E2E against `.devimport` (2 series, 5 CBZs).
- **MangaFire source rewritten against the real site** (2026-07). The old scraper targeted a dead markup/domain (`mangafire.io`, `/manga/{slug}.{code}` URLs); the real `mangafire.to` is a React SPA whose HTML is an empty shell, with a clean JSON API underneath. New source uses `/api/titles?keyword=` (search), `/api/titles/{hid}` (detail), `/api/titles/{hid}/chapters?language=&limit=200&page=` (paginated, server-side language filter), `/api/chapters/{id}` (pages). Series id format changed to the `/title/` URL tail (`{hid}-{slug}`) — any previously saved mangafire mappings must be relinked. Pages are served unscrambled on the new site (`MangaFireDescrambler` kept for `ScrambleOffset > 0`). Official and unofficial rips of the same chapter are deduped preferring official. FlareSolverr-fetched JSON arrives `<pre>`-wrapped and is unwrapped. Verified E2E: search → link → 152 Frieren chapters synced → ch. 140.1 downloaded as a valid 21-page CBZ.
- **MangaPill covers broken when linking a source** (2026-07). Source-search covers were hotlinked by the browser and the CDN 403s requests without the site's own Referer. `SearchController` now rewrites `coverUrl` through a new `/api/v1/search/cover` proxy endpoint that fetches with `Referer: {source.BaseUrl}/` (apikey rides in the query string since `<img>` can't send headers). Applies to all sources' search results.
- **Unchecking items during import errored out** (2026-07). Same `e.currentTarget`-inside-`setState`-updater bug as the settings page: React nulls `currentTarget` after the handler, so unticking crashed. `checked` is now captured before the state update. Verified in the browser.
- **ComicInfo updater on adopted CBZs** (2026-07). `ComicInfoUpdater` (Core) rewrites the ComicInfo.xml inside every CBZ adopted by `CbzLinkService` (library import, completed torrents, rescan): series-level fields standardized to Mangarr metadata (English title, summary, authors, genres) so Kavita groups imports with downloads; file-level fields kept or filled from the linked chapter / parsed name; unmanaged fields (Publisher, Translator, ...) preserved. Atomic rewrite, no-op when already standard.
- **MangaFire descrambling** (2026-07). `PageRequest.ScrambleOffset` carries the ajax offset; `PageDownloader` descrambles at download time via `MangaFireDescrambler` (Core, ImageSharp port of the retired Tachiyomi ImageInterceptor: ≤200px tiles, ≥5×5 grid, offset-modulo shuffle with the last row/column fixed). Round-trip pixel tests in Core.Tests.
- **Settings page crashed when typing in Prowlarr/qBittorrent fields** (2026-07). `ConnectionSettingsCard` read `e.currentTarget` inside the `setState` updater callback, which runs after React nulls `currentTarget`. Value is now captured before the state update.
